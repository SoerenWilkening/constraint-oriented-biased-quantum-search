---
phase: 24-phase-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-dead-code-removal/VERIFICATION.md
autonomous: true
requirements: [DEAD-01, DEAD-02, DEAD-03, DEAD-04]

must_haves:
  truths:
    - "VERIFICATION.md exists for Phase 18 confirming all 4 DEAD requirements are satisfied"
    - "Each DEAD requirement has explicit evidence from the current codebase (grep output, file inspection)"
    - "No orphaned model_t fields remain in model.h, model.c, or Model.pxd"
    - "No commented-out function signatures remain in solver.h"
    - "No commented-out code blocks remain in local_search.c"
  artifacts:
    - path: ".planning/phases/18-dead-code-removal/VERIFICATION.md"
      provides: "Phase 18 verification report confirming DEAD-01 through DEAD-04"
      contains: "DEAD-01"
  key_links:
    - from: "VERIFICATION.md"
      to: "REQUIREMENTS.md"
      via: "requirement ID references"
      pattern: "DEAD-0[1-4]"
---

<objective>
Create VERIFICATION.md for Phase 18 (Dead Code Removal), confirming that requirements DEAD-01 through DEAD-04 are fully satisfied in the current codebase.

Purpose: Close the verification gap for Phase 18 -- the implementation was completed in Phase 18 but no VERIFICATION.md was produced. This plan produces the missing verification artifact.
Output: .planning/phases/18-dead-code-removal/VERIFICATION.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-dead-code-removal/18-01-SUMMARY.md
@.planning/phases/21-build-packaging/21-VERIFICATION.md (format reference)

Phase 18 Success Criteria (from ROADMAP.md):
1. The 4 orphaned model_t fields (manual_bias, bias_factor, manual_bias_factor, look_ahead_factor) are absent from model.h, model.c, and Model.pxd -- compiler and grep confirm no references
2. Model.pxd contains no Cython declarations for fields that no longer exist in the C struct
3. solver.h contains no commented-out function signatures
4. local_search.c contains no commented-out code blocks
5. Full test suite (56 C + 390 Python) passes after removal with zero new failures

Key files to verify against:
- cbqs/src/model.h (model_t struct definition)
- cbqs/src/model.c (model init/free)
- cbqs/Model.pxd (Cython declarations)
- cbqs/src/solver.h (function signatures)
- cbqs/src/local_search.c (code blocks)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify DEAD-01 through DEAD-04 and produce VERIFICATION.md</name>
  <files>.planning/phases/18-dead-code-removal/VERIFICATION.md</files>
  <action>
Run verification checks against the current codebase for each DEAD requirement:

**DEAD-01: All 4 orphaned model_t fields removed from model.h, model.c, and Model.pxd**
- Run: `grep -n 'manual_bias\|bias_factor\|manual_bias_factor\|look_ahead_factor' cbqs/src/model.h cbqs/src/model.c cbqs/Model.pxd`
- Expected: No matches (the 4 orphaned fields are absent). Note: `bias_factor` may appear in solver_ctx or branching_stats context -- that is a DIFFERENT field (active, not orphaned). Only model_t references count.
- Actually grep for these field names specifically as model_t struct members. Check model.h for `typedef struct` block and confirm the 4 fields are absent.

**DEAD-02: Orphaned Cython declarations removed from Model.pxd**
- Run: `grep -n 'manual_bias\|manual_bias_factor' cbqs/Model.pxd`
- Expected: No matches. These were orphaned Cython declarations that mapped to removed C fields.

**DEAD-03: Commented-out function signature in solver.h removed**
- Run: `grep -n '^\s*//' cbqs/src/solver.h` to check for any commented-out lines
- Inspect: Confirm no commented-out function signatures (specifically the initial_state_preparation signature that was removed in Phase 18)

**DEAD-04: Commented-out code blocks in local_search.c removed**
- Run: `grep -n '^\s*//' cbqs/src/local_search.c | head -30` to check for commented-out code
- Specifically verify: no stale constraint_violation loop, no objective_value else branch, no objective_value_improved block remain as commented-out code
- Note: Legitimate algorithm comments (added in Phase 22) are expected -- distinguish between documentation comments and commented-out code

**Test suite:**
- Check that the test suite passes by referencing the 18-01-SUMMARY.md and subsequent phase summaries that confirmed passing tests
- Note in VERIFICATION.md that direct test execution requires a build environment

Write the VERIFICATION.md following the format established in Phase 21's verification report:
- YAML frontmatter with phase, verified timestamp, status, score
- Goal Achievement section with Observable Truths table
- Requirements Coverage section mapping each DEAD-XX to evidence
- Gaps Summary confirming no gaps
- Human Verification Required section for items needing a build environment
  </action>
  <verify>
    <automated>test -f .planning/phases/18-dead-code-removal/VERIFICATION.md && grep -c 'DEAD-01' .planning/phases/18-dead-code-removal/VERIFICATION.md && grep -c 'DEAD-02' .planning/phases/18-dead-code-removal/VERIFICATION.md && grep -c 'DEAD-03' .planning/phases/18-dead-code-removal/VERIFICATION.md && grep -c 'DEAD-04' .planning/phases/18-dead-code-removal/VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md exists in Phase 18 directory, contains verification evidence for all 4 DEAD requirements (DEAD-01, DEAD-02, DEAD-03, DEAD-04), and each requirement has a PASSED/FAILED status with grep-based evidence from the current codebase</done>
</task>

</tasks>

<verification>
- .planning/phases/18-dead-code-removal/VERIFICATION.md exists
- File contains all 4 requirement IDs: DEAD-01, DEAD-02, DEAD-03, DEAD-04
- Each requirement has a status (PASSED/FAILED) and evidence
- File follows the established verification report format (frontmatter + truths table + requirements coverage)
</verification>

<success_criteria>
Phase 18 VERIFICATION.md confirms DEAD-01 through DEAD-04 are satisfied with evidence from the current codebase (grep outputs and file inspection). The verification gap for Phase 18 is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/24-phase-verification/24-01-SUMMARY.md`
</output>
