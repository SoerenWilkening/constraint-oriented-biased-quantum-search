---
phase: 24-phase-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-incremental-evaluation/VERIFICATION.md
autonomous: true
requirements: [INCR-01, INCR-02, INCR-03]

must_haves:
  truths:
    - "VERIFICATION.md exists for Phase 19 confirming all 3 INCR requirements are satisfied"
    - "INCR-01 confirmed: benchmark results exist with before/after wall-clock metrics"
    - "INCR-02 confirmed: local_search uses incremental evaluation (adjusted_constraint_violation or equivalent)"
    - "INCR-03 confirmed: all tests pass after incremental adoption with no correctness regression"
  artifacts:
    - path: ".planning/phases/19-incremental-evaluation/VERIFICATION.md"
      provides: "Phase 19 verification report confirming INCR-01 through INCR-03"
      contains: "INCR-01"
  key_links:
    - from: "VERIFICATION.md"
      to: "REQUIREMENTS.md"
      via: "requirement ID references"
      pattern: "INCR-0[1-3]"
---

<objective>
Create VERIFICATION.md for Phase 19 (Incremental Evaluation), confirming that requirements INCR-01 through INCR-03 are fully satisfied in the current codebase.

Purpose: Close the verification gap for Phase 19 -- the implementation and benchmark were completed but no VERIFICATION.md was produced. This plan produces the missing verification artifact.
Output: .planning/phases/19-incremental-evaluation/VERIFICATION.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-incremental-evaluation/19-01-SUMMARY.md
@.planning/phases/19-incremental-evaluation/19-02-SUMMARY.md
@.planning/phases/19-incremental-evaluation/BENCHMARK.md
@.planning/phases/21-build-packaging/21-VERIFICATION.md (format reference)

Phase 19 Success Criteria (from ROADMAP.md):
1. Before/after benchmark results exist showing wall-clock time for representative problem sizes under both full-recalc and incremental evaluation paths
2. local_search calls adjusted_constraint_violation() (or equivalent incremental path) instead of full constraint recalculation for objective evaluation on each move
3. All 446 tests pass after the incremental adoption -- no correctness regression
4. Benchmark output (or summary) is committed alongside the implementation change

Key files to verify against:
- cbqs/src/local_search.c (incremental evaluation in explore_neighbourhood and accept_best_routine)
- tests/test_constraint.c (test_incremental_vs_full_recalc correctness test)
- benchmarks/benchmark_incremental.py (benchmark script)
- .planning/phases/19-incremental-evaluation/BENCHMARK.md (benchmark results)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify INCR-01 through INCR-03 and produce VERIFICATION.md</name>
  <files>.planning/phases/19-incremental-evaluation/VERIFICATION.md</files>
  <action>
Run verification checks against the current codebase for each INCR requirement:

**INCR-01: Before/after benchmark results exist with wall-clock metrics**
- Verify `benchmarks/benchmark_incremental.py` exists and contains benchmark logic
- Verify `.planning/phases/19-incremental-evaluation/BENCHMARK.md` exists and contains timing results
- Read BENCHMARK.md and confirm it has a results table with wall-clock times for multiple problem sizes
- Note: The benchmark measures the incremental-evaluation code path (there is no separate "before" commit benchmark since the project does not maintain a pre-incremental branch). The BENCHMARK.md documents timing results and methodology for cross-commit comparison.

**INCR-02: Incremental evaluation adopted in local_search**
- Run: `grep -n 'adjusted_constraint_violation\|prepare_constraints' cbqs/src/local_search.c`
- Expected: Multiple matches showing that explore_neighbourhood() and accept_best_routine() use incremental delta evaluation
- Verify that `constraint_violation()` loop is NOT the primary evaluation path in per-move exploration (it may still exist for baseline refresh after accepted moves, which is correct per the design decision)
- Check for `remainings` array management in local_search() function

**INCR-03: All tests pass with no correctness regression**
- Verify `tests/test_constraint.c` contains `test_incremental_vs_full_recalc` test function
- Run: `grep -n 'test_incremental_vs_full_recalc' tests/test_constraint.c`
- Reference 19-01-SUMMARY.md for test suite results (446 tests passing)
- Note in VERIFICATION.md that direct test execution requires a build environment

Write the VERIFICATION.md following the format established in Phase 21's verification report:
- YAML frontmatter with phase, verified timestamp, status, score
- Goal Achievement section with Observable Truths table
- Requirements Coverage section mapping each INCR-XX to evidence
- Artifacts section confirming benchmark script and results exist
- Gaps Summary confirming no gaps
- Human Verification Required section for items needing a build environment
  </action>
  <verify>
    <automated>test -f .planning/phases/19-incremental-evaluation/VERIFICATION.md && grep -c 'INCR-01' .planning/phases/19-incremental-evaluation/VERIFICATION.md && grep -c 'INCR-02' .planning/phases/19-incremental-evaluation/VERIFICATION.md && grep -c 'INCR-03' .planning/phases/19-incremental-evaluation/VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md exists in Phase 19 directory, contains verification evidence for all 3 INCR requirements (INCR-01, INCR-02, INCR-03), and each requirement has a PASSED/FAILED status with grep-based evidence and benchmark artifact references</done>
</task>

</tasks>

<verification>
- .planning/phases/19-incremental-evaluation/VERIFICATION.md exists
- File contains all 3 requirement IDs: INCR-01, INCR-02, INCR-03
- Each requirement has a status (PASSED/FAILED) and evidence
- Benchmark artifacts (script + results) are referenced
- File follows the established verification report format
</verification>

<success_criteria>
Phase 19 VERIFICATION.md confirms INCR-01 through INCR-03 are satisfied with evidence from the current codebase (grep outputs, benchmark file references, test existence). The verification gap for Phase 19 is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/24-phase-verification/24-02-SUMMARY.md`
</output>
