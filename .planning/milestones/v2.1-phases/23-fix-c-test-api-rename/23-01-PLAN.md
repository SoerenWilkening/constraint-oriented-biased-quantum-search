---
phase: 23-fix-c-test-api-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_branching.c, tests/test_thread_safety.c]
autonomous: true
requirements: [API-01]

must_haves:
  truths:
    - "All 11 calls to solver_ctx_set_look_factor() in tests/test_branching.c are replaced with solver_ctx_set_look_ahead_factor()"
    - "The 1 access to ctx->branching_stats.look_factor in tests/test_branching.c is replaced with ctx->branching_stats.look_ahead_factor"
    - "The 1 call to solver_ctx_set_look_factor() in tests/test_thread_safety.c is replaced with solver_ctx_set_look_ahead_factor()"
    - "Both tests/test_branching.c and tests/test_thread_safety.c compile without errors against current headers"
    - "All C tests pass after the rename (cmake --build + ctest)"
  artifacts:
    - path: "tests/test_branching.c"
      provides: "Updated test file using look_ahead_factor API"
    - path: "tests/test_thread_safety.c"
      provides: "Updated test file using look_ahead_factor API"
  key_links:
    - from: "solver_ctx_set_look_ahead_factor"
      to: "ctx->branching_stats.look_ahead_factor"
      via: "setter writes to struct field"
      pattern: "solver_ctx_set_look_ahead_factor"
---

<objective>
Rename all occurrences of the old `look_factor` API to `look_ahead_factor` in the C test files so they compile and pass against the current headers after the Phase 20 rename.

Purpose: Close the integration gap between Phase 20 (API Consistency) and the C test suite. Phase 20 renamed `look_factor` to `look_ahead_factor` in the C headers, Cython bindings, and Python layer, but the C test files were not updated.
Output: Updated test files that compile and pass with zero errors.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@tests/test_branching.c
@tests/test_thread_safety.c
@cbqs/src/solver_ctx.h (line 170: current API declaration)
@cbqs/src/Branching.h (line 43: current struct field name)
@tests/CMakeLists.txt
</context>

<interfaces>
<!-- Current API (after Phase 20 rename) -->

1. Header declaration (solver_ctx.h:170):
   `void solver_ctx_set_look_ahead_factor(solver_ctx_t *ctx, double factor);`

2. Struct field (Branching.h:43):
   `double look_ahead_factor;` inside `BranchingStats_t`

<!-- Old API (what the test files still use) -->

3. test_branching.c — 11 calls to `solver_ctx_set_look_factor()`:
   Lines 42, 79, 95, 112, 128, 144, 176, 247, 275, 304, 365

4. test_branching.c — 1 struct field access `ctx->branching_stats.look_factor`:
   Line 45

5. test_branching.c — 8 comment references to `look_factor`:
   Lines 72, 153, 156, 164, 165, 297, 306, 348

6. test_thread_safety.c — 1 call to `solver_ctx_set_look_factor()`:
   Line 213
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename look_factor to look_ahead_factor in test_branching.c</name>
  <files>tests/test_branching.c</files>
  <action>
In tests/test_branching.c, perform the following replacements:

1. Replace all 11 function calls:
   `solver_ctx_set_look_factor(` -> `solver_ctx_set_look_ahead_factor(`
   Locations: lines 42, 79, 95, 112, 128, 144, 176, 247, 275, 304, 365

2. Replace the 1 struct field access:
   `ctx->branching_stats.look_factor` -> `ctx->branching_stats.look_ahead_factor`
   Location: line 45

3. Update the 8 comment references for consistency:
   `look_factor` -> `look_ahead_factor` in comments at lines 72, 153, 156, 164, 165, 297, 306, 348

Use a global find-and-replace of `look_factor` -> `look_ahead_factor` across the entire file. This is safe because every occurrence of `look_factor` in this file (whether in code or comments) refers to the same concept and should be renamed.

Do NOT change `branching_factor` or `bias_factor` — only `look_factor` (which is never a substring of those names).
  </action>
  <verify>
    <automated>python3 -c "
import subprocess
# Verify no old API references remain
with open('tests/test_branching.c') as f:
    content = f.read()
# Count old vs new
old_func = content.count('solver_ctx_set_look_factor(')
old_field = content.count('branching_stats.look_factor')
new_func = content.count('solver_ctx_set_look_ahead_factor(')
new_field = content.count('branching_stats.look_ahead_factor')
print(f'Old function calls: {old_func} (expected 0)')
print(f'Old field accesses: {old_field} (expected 0)')
print(f'New function calls: {new_func} (expected 11)')
print(f'New field accesses: {new_field} (expected 1)')
assert old_func == 0, f'Found {old_func} old solver_ctx_set_look_factor() calls'
assert old_field == 0, f'Found {old_field} old branching_stats.look_factor accesses'
assert new_func == 11, f'Expected 11 new function calls, got {new_func}'
assert new_field == 1, f'Expected 1 new field access, got {new_field}'
# Ensure branching_factor and bias_factor are untouched
assert content.count('solver_ctx_set_branching_factor(') > 0, 'branching_factor calls missing'
assert content.count('solver_ctx_set_bias_factor(') > 0, 'bias_factor calls missing'
print('All assertions passed')
"
    </automated>
  </verify>
  <done>All 11 calls to solver_ctx_set_look_factor() replaced with solver_ctx_set_look_ahead_factor(). The 1 struct field access replaced. All 8 comment references updated. No old look_factor references remain. branching_factor and bias_factor are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rename look_factor in test_thread_safety.c and verify compilation</name>
  <files>tests/test_thread_safety.c</files>
  <action>
1. In tests/test_thread_safety.c, replace the 1 function call:
   `solver_ctx_set_look_factor(ctx, 4.0)` -> `solver_ctx_set_look_ahead_factor(ctx, 4.0)`
   Location: line 213

2. After both Task 1 and Task 2 edits are complete, verify compilation and test execution:
   ```
   cd tests
   rm -rf build-fresh && mkdir build-fresh && cd build-fresh
   cmake .. -DWERROR=ON
   cmake --build . --target test_branching --target test_thread_safety 2>&1
   ```
   Both targets must compile with zero errors and zero warnings (WERROR=ON).

3. Run the tests:
   ```
   cd tests/build-fresh
   ctest -R "test_branching|test_thread_safety" --output-on-failure
   ```
   Both tests must pass.

4. Clean up the build directory:
   ```
   rm -rf tests/build-fresh
   ```
  </action>
  <verify>
    <automated>cd tests && rm -rf build-verify && mkdir build-verify && cd build-verify && cmake .. -DWERROR=ON 2>&1 && cmake --build . --target test_branching --target test_thread_safety 2>&1 && ctest -R "test_branching|test_thread_safety" --output-on-failure 2>&1 && cd ../.. && rm -rf tests/build-verify</automated>
  </verify>
  <done>test_thread_safety.c updated. Both test_branching and test_thread_safety compile with -Werror and pass all tests. No old look_factor references remain in the test suite.</done>
</task>

</tasks>

<verification>
1. `grep -c 'solver_ctx_set_look_factor(' tests/test_branching.c` returns 0
2. `grep -c 'solver_ctx_set_look_ahead_factor(' tests/test_branching.c` returns 11
3. `grep -c 'branching_stats.look_factor' tests/test_branching.c` returns 0
4. `grep -c 'branching_stats.look_ahead_factor' tests/test_branching.c` returns 1
5. `grep -c 'solver_ctx_set_look_factor(' tests/test_thread_safety.c` returns 0
6. Both test_branching and test_thread_safety compile with -Werror and pass via ctest
</verification>

<success_criteria>
- Zero occurrences of `solver_ctx_set_look_factor(` in tests/test_branching.c
- Zero occurrences of `branching_stats.look_factor` in tests/test_branching.c
- Exactly 11 occurrences of `solver_ctx_set_look_ahead_factor(` in tests/test_branching.c
- Exactly 1 occurrence of `branching_stats.look_ahead_factor` in tests/test_branching.c
- Zero occurrences of `solver_ctx_set_look_factor(` in tests/test_thread_safety.c
- Both tests compile with -Werror against current headers
- Both tests pass via ctest
</success_criteria>

<output>
After completion, create `.planning/phases/23-fix-c-test-api-rename/23-01-SUMMARY.md`
</output>
