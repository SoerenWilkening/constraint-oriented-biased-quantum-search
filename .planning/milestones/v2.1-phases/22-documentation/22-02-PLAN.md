---
phase: 22-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [cbqs/Expression.pyx, cbqs/Constraint.pyx]
autonomous: true
requirements: [DOC-02]

must_haves:
  truths:
    - "Every public method on the Expression class has a NumPy-style docstring"
    - "Every public method on the Constraint (new_constraint) class has a NumPy-style docstring"
    - "help(Expression) and help(new_constraint) produce readable output"
    - "Non-obvious dunders (__add__, __mul__, __le__, __ge__, __eq__) have docstrings explaining Expression combining"
  artifacts:
    - path: "cbqs/Expression.pyx"
      provides: "Expression class and Variable class with complete docstrings"
    - path: "cbqs/Constraint.pyx"
      provides: "new_constraint class with complete docstrings"
  key_links:
    - from: "Variable.__add__"
      to: "Expression creation"
      via: "operator overloading"
      pattern: "def __add__"
    - from: "Expression.__le__"
      to: "Constraint creation"
      via: "comparison operator"
      pattern: "def __le__"
---

<objective>
Add NumPy-style docstrings to every public method on the Expression and Constraint (new_constraint) classes, plus the Variable helper class.

Purpose: Satisfy DOC-02 so pydoc/help() produces readable output for all Expression and Constraint methods.
Output: Updated cbqs/Expression.pyx and cbqs/Constraint.pyx with complete documentation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-documentation/22-CONTEXT.md
@cbqs/Expression.pyx
@cbqs/Constraint.pyx
</context>

<interfaces>
<!-- Classes and methods requiring docstrings -->

Variable class (in Expression.pyx):
- __init__(index, name, lb, ub, vtype) -- binary/integer decision variable
- __add__, __radd__ -- create Expression from Variable + int
- __mul__, __rmul__ -- create Expression from Variable * int

Expression class (Cython cdef class in Expression.pyx):
- __copy__, __deepcopy__ -- copy semantics (important: copies C struct)
- merge() -- merge duplicate variable terms
- __iter__ -- iterate over terms
- __add__, __radd__, __iadd__ -- combine expressions / add constants
- __isub__ -- subtract constant from expression
- __mul__, __rmul__, __imul__ -- scalar multiplication
- __le__ -- creates constraint: expr <= rhs (returns Expression with constraint flag)
- __ge__ -- creates constraint: expr >= rhs
- __eq__ -- creates constraint: expr == rhs
- Class-level docstring: purpose, how expressions represent linear/polynomial objectives

new_constraint class (in Constraint.pyx):
- __copy__ -- copy semantics
- __len__ -- number of constraints
- process(n, enforce_density) -- preprocess constraints for solving
- add_expression(expr) -- add an Expression as a constraint
- eval_con(state) -- evaluate constraint violations for a state
- eval_con_from_array(array) -- evaluate constraint violations from array
- eval_obj(state) -- evaluate objective value for a state
- Class-level docstring: purpose, internal constraint container
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add docstrings to Variable and Expression classes</name>
  <files>cbqs/Expression.pyx</files>
  <action>
Add NumPy-style docstrings to the Variable class and Expression class in Expression.pyx. Follow CONTEXT.md decisions:
- NumPy-style format with Parameters, Returns, Raises sections
- Types in Parameters section
- Thin wrappers get full docstrings from user perspective
- Non-obvious dunders (__add__, __mul__, __le__, __ge__, __eq__) get docstrings explaining Expression combining
- Skip standard dunders (__repr__, __str__, __init__ on Expression)

For Variable class:
1. Class-level docstring: represents a binary/integer decision variable with index, name, bounds
2. __init__: document parameters (index, name, lb, ub, vtype)
3. __add__, __radd__: "Create an Expression by adding a constant to this variable"
4. __mul__, __rmul__: "Create an Expression by multiplying this variable by a coefficient"

For Expression class:
1. Class-level docstring: represents a linear/polynomial expression over decision variables. Expressions are built using operator overloading (Variable + Variable, Expression * scalar, etc.). Used as objectives and constraints in the Model.
2. __copy__: documents that it copies the underlying C expression_t struct
3. __deepcopy__: same behavior as __copy__ (C struct is value-copied)
4. merge(): merges duplicate variable terms, summing coefficients
5. __add__, __radd__: combining expressions or adding constants; returns NEW expression (immutability semantics)
6. __iadd__: in-place addition (mutates self)
7. __isub__: in-place subtraction of constant
8. __mul__, __rmul__: scalar multiplication; returns NEW expression
9. __imul__: in-place scalar multiplication
10. __le__: creates a <= constraint expression (used with Model.add_constraint)
11. __ge__: creates a >= constraint expression
12. __eq__: creates an == constraint expression

Include Examples for __le__/__ge__/__eq__ showing constraint creation pattern:
```python
>>> x = model.add_variables(3)
>>> model.add_constraint(x[0] + x[1] + x[2] <= 2)
```
  </action>
  <verify>
    <automated>python3 -c "
import re
with open('cbqs/Expression.pyx') as f:
    src = f.read()
# Check Variable class methods
var_src = src[src.index('class Variable'):src.index('cdef class Expression') if 'cdef class Expression' in src else len(src)]
var_methods = re.findall(r'^\t(?:def|cpdef)\s+(\w+)\s*\(', var_src, re.MULTILINE)
var_public = [m for m in var_methods if not m.startswith('_') or m in ('__add__', '__radd__', '__mul__', '__rmul__', '__init__')]
# Check Expression class
expr_idx = src.index('class Expression')
expr_src = src[expr_idx:]
expr_methods = re.findall(r'^\t(?:def|cpdef)\s+(\w+)\s*\(', expr_src, re.MULTILINE)
expr_need_docs = [m for m in expr_methods if not m.startswith('_') or m in ('__copy__', '__deepcopy__', '__add__', '__radd__', '__iadd__', '__isub__', '__mul__', '__rmul__', '__imul__', '__le__', '__ge__', '__eq__')]
print(f'Variable methods to check: {var_public}')
print(f'Expression methods to check: {expr_need_docs}')
missing = []
for m in expr_need_docs:
    pattern = rf'def\s+{re.escape(m)}\s*\([^)]*\)[^:]*:'
    match = re.search(pattern, expr_src)
    if match:
        after = expr_src[match.end():match.end()+200].lstrip()
        if not (after.startswith('\"\"\"') or after.startswith(\"'''\")):
            missing.append(f'Expression.{m}')
if missing:
    print(f'Missing docstrings: {missing}')
    assert False, f'{len(missing)} methods lack docstrings'
print(f'All Expression methods documented')
# Check class-level docstrings
assert '\"\"\"' in expr_src[:300], 'Expression class docstring missing'
assert '\"\"\"' in var_src[:300], 'Variable class docstring missing'
print('Class docstrings present')
"
    </automated>
  </verify>
  <done>Variable and Expression classes have class-level docstrings and all public + non-obvious dunder methods have NumPy-style docstrings.</done>
</task>

<task type="auto">
  <name>Task 2: Add docstrings to new_constraint class</name>
  <files>cbqs/Constraint.pyx</files>
  <action>
Add NumPy-style docstrings to the new_constraint class in Constraint.pyx. Follow CONTEXT.md decisions:
- NumPy-style format with Parameters, Returns, Raises sections
- Thin wrappers get full docstrings from user perspective
- Non-obvious dunders (__copy__, __len__) get docstrings

1. Class-level docstring: container for constraint expressions used by the solver. Constraints are added via Model.add_constraint() — direct use of this class is rarely needed. Documents purpose and typical internal usage.

2. Methods to document:
   - __copy__: returns a deep copy of the constraint set including all C data
   - __len__: returns the number of constraint expressions in this container
   - process(n, enforce_density): preprocesses constraints for solving; chooses dense or sparse representation based on clause density. Parameters: n (int, number of variables), enforce_density (bool, force dense representation). Returns DENSE or SPARSE constant.
   - add_expression(expr): adds an Expression to this constraint set. Parameter: expr (Expression).
   - eval_con(state): evaluates constraint violations for a given state. Returns violation count.
   - eval_con_from_array(array): evaluates constraints from a Python list assignment. Creates temporary state internally.
   - eval_obj(state): evaluates the objective function value for a given state.
  </action>
  <verify>
    <automated>python3 -c "
import re
with open('cbqs/Constraint.pyx') as f:
    src = f.read()
con_src = src[src.index('class new_constraint'):]
methods = re.findall(r'^\t(?:def|cpdef)\s+(\w+)\s*\(', con_src, re.MULTILINE)
need_docs = [m for m in methods if not m.startswith('_') or m in ('__copy__', '__len__')]
missing = []
for m in need_docs:
    pattern = rf'def\s+{re.escape(m)}\s*\([^)]*\)[^:]*:'
    match = re.search(pattern, con_src)
    if match:
        after = con_src[match.end():match.end()+200].lstrip()
        if not (after.startswith('\"\"\"') or after.startswith(\"'''\")):
            missing.append(m)
if missing:
    print(f'Missing docstrings: {missing}')
    assert False, f'{len(missing)} methods lack docstrings'
print(f'All {len(need_docs)} new_constraint methods documented')
# Check class docstring
assert '\"\"\"' in con_src[:300], 'new_constraint class docstring missing'
print('Class docstring present')
"
    </automated>
  </verify>
  <done>new_constraint class has a class-level docstring and all public + non-obvious dunder methods have NumPy-style docstrings. help(new_constraint) produces readable output.</done>
</task>

</tasks>

<verification>
1. `python3 -c "from cbqs.Expression import Variable, Expression; help(Variable); help(Expression)"` produces readable output
2. `python3 -c "from cbqs.Constraint import new_constraint; help(new_constraint)"` produces readable output
3. No existing tests broken: `python3 -m pytest tests/ -x -q` passes
</verification>

<success_criteria>
- All public methods on Expression and Variable have NumPy-style docstrings
- All public methods on new_constraint have NumPy-style docstrings
- Non-obvious dunders (__add__, __mul__, __le__, __ge__, __eq__, __copy__, __len__) documented
- Class-level docstrings present for Variable, Expression, new_constraint
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-documentation/22-02-SUMMARY.md`
</output>
