---
phase: 03-solver-context-architecture
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - cbqs/src/solver.h
  - cbqs/src/solver.c
  - cbqs/src/approximate_state_sampler.h
  - cbqs/src/approximate_state_sampler.c
  - cbqs/src/Branching.h
  - cbqs/src/Branching.c
autonomous: true

must_haves:
  truths:
    - "CSearch_opt, CSearch_opt_sat, CSearch_sat take solver_ctx_t* as first parameter"
    - "All BranchingFunction calls use &ctx->branching_stats instead of &BranchingStats"
    - "Monte carlo sampler functions take solver_ctx_t* parameter"
    - "approximate_state_sampler functions use ctx->branching_stats"
    - "StateProbability and updated() take solver_ctx_t* and use ctx->branching_stats"
    - "No references to global BranchingStats in migrated functions"
  artifacts:
    - path: "cbqs/src/solver.h"
      provides: "Updated function signatures with ctx parameter"
      contains: "solver_ctx_t *ctx"
    - path: "cbqs/src/solver.c"
      provides: "Migrated solver functions"
      contains: "&ctx->branching_stats"
    - path: "cbqs/src/Branching.c"
      provides: "Migrated StateProbability and updated with ctx"
      contains: "ctx->branching_stats"
  key_links:
    - from: "cbqs/src/solver.c"
      to: "cbqs/src/solver_ctx.h"
      via: "#include"
      pattern: "#include.*solver_ctx\\.h"
    - from: "cbqs/src/solver.c"
      to: "ctx->branching_stats"
      via: "BranchingFunction calls"
      pattern: "BranchingFunction.*&ctx->branching_stats"
    - from: "cbqs/src/Branching.c"
      to: "ctx->branching_stats"
      via: "StateProbability implementation"
      pattern: "ctx->branching_stats"
---

<objective>
Migrate leaf-level solver functions (CSearch_*, monte carlo samplers, StateProbability, updated) to accept solver_ctx_t* and use ctx->branching_stats instead of global BranchingStats.

Purpose: This is the core migration of leaf functions that call BranchingFunction. Following bottom-up migration order, these must be done before entry points (ctg, local_search) so the call chain compiles.

Output: Updated solver.h/c, approximate_state_sampler.h/c, and Branching.h/c with ctx parameter threading.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-solver-context-architecture/03-CONTEXT.md
@.planning/phases/03-solver-context-architecture/03-RESEARCH.md
@.planning/phases/03-solver-context-architecture/03-01-SUMMARY.md
@cbqs/src/solver.h
@cbqs/src/solver.c
@cbqs/src/approximate_state_sampler.h
@cbqs/src/approximate_state_sampler.c
@cbqs/src/Branching.h
@cbqs/src/Branching.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update solver.h with ctx parameter in function signatures</name>
  <files>cbqs/src/solver.h</files>
  <action>
1. Add #include "solver_ctx.h" at the top (after existing includes)

2. Update function signatures to add solver_ctx_t *ctx as FIRST parameter:

   Before:
   int CSearch_opt(state_t *cur_sol, int j, new_constraints_t *con, new_constraints_t *obj,
                   int depth_look_ahead, int direction, array_t *ful, int *samples);

   After:
   int CSearch_opt(solver_ctx_t *ctx, state_t *cur_sol, int j, new_constraints_t *con,
                   new_constraints_t *obj, int depth_look_ahead, int direction,
                   array_t *ful, int *samples);

3. Update ALL solver function signatures:
   - CSearch_opt
   - CSearch_opt_sat
   - CSearch_sat
   - CSearch_opt_monte_carlo_sampler
   - CSearch_opt_sat_monte_carlo_sampler
   - CSearch_sat_monte_carlo_sampler
   - initial_state_preparation (takes model_t which will get ctx later, but prepare signature)

Note: look_ahead_correct and update_potentials don't call BranchingFunction directly, so they don't need ctx (they're pure constraint evaluation).
  </action>
  <verify>Header file has solver_ctx_t *ctx in all CSearch_* function declarations</verify>
  <done>All function signatures updated with ctx as first parameter</done>
</task>

<task type="auto">
  <name>Task 2: Migrate solver.c functions to use ctx->branching_stats</name>
  <files>cbqs/src/solver.c</files>
  <action>
1. Add #include "solver_ctx.h" at the top

2. Update CSearch_opt function:
   - Add solver_ctx_t *ctx as first parameter
   - Find the BranchingFunction call (search for "BranchingFunction" in the function body)
   - Change: BranchingFunction(i, bit, 0, 0, &BranchingStats)
     to: BranchingFunction(i, bit, 0, 0, &ctx->branching_stats)

3. Update CSearch_opt_sat function:
   - Add solver_ctx_t *ctx as first parameter
   - Find the BranchingFunction call in the function body
   - Change: BranchingFunction(i, bit, 0, 0, &BranchingStats)
     to: BranchingFunction(i, bit, 0, 0, &ctx->branching_stats)

4. Update CSearch_sat function:
   - Add solver_ctx_t *ctx as first parameter
   - Find the BranchingFunction call in the function body
   - Change: BranchingFunction(i, bit, 0, 0, &BranchingStats)
     to: BranchingFunction(i, bit, 0, 0, &ctx->branching_stats)

5. Update CSearch_opt_monte_carlo_sampler:
   - Add solver_ctx_t *ctx as first parameter
   - Find the BranchingFunction call in the function body
   - Change: BranchingFunction(i, bit, 0, 0, &BranchingStats)
     to: BranchingFunction(i, bit, 0, 0, &ctx->branching_stats)

6. Update CSearch_opt_sat_monte_carlo_sampler:
   - Add solver_ctx_t *ctx as first parameter
   - Find the BranchingFunction call in the function body
   - Change: BranchingFunction(i, bit, 0, 0, &BranchingStats)
     to: BranchingFunction(i, bit, 0, 0, &ctx->branching_stats)

7. Update CSearch_sat_monte_carlo_sampler:
   - Add solver_ctx_t *ctx as first parameter
   - Find the BranchingFunction call in the function body
   - Change: BranchingFunction(i, bit, 0, 0, &BranchingStats)
     to: BranchingFunction(i, bit, 0, 0, &ctx->branching_stats)

8. Verify no remaining references to &BranchingStats in solver.c (grep for it)

Note: initial_state_preparation takes model_t* -- it doesn't directly call BranchingFunction. It will be updated when model_t gets ctx, but for now leave it.
  </action>
  <verify>grep -n "BranchingStats" cbqs/src/solver.c shows no hits (all replaced with ctx->branching_stats)</verify>
  <done>All BranchingFunction calls use &ctx->branching_stats</done>
</task>

<task type="auto">
  <name>Task 3: Update approximate_state_sampler and Branching.c leaf functions</name>
  <files>cbqs/src/approximate_state_sampler.h, cbqs/src/approximate_state_sampler.c, cbqs/src/Branching.h, cbqs/src/Branching.c</files>
  <action>
PART A - approximate_state_sampler:

1. Update approximate_state_sampler.h:
   - Add #include "solver_ctx.h"
   - Update init_approximete_state signature to take solver_ctx_t *ctx:
     Before: approximate_state_t *init_approximete_state(int n, double bias);
     After: approximate_state_t *init_approximete_state(solver_ctx_t *ctx, int n, double bias);
   - Update CSearch_opt_sampler signature:
     Before: int CSearch_opt_sampler(approximate_state_t *state, state_t *cur_sol, int samples,
                                     new_constraints_t *con, new_constraints_t *obj, int depth_look_ahead);
     After: int CSearch_opt_sampler(solver_ctx_t *ctx, approximate_state_t *state, state_t *cur_sol,
                                    int samples, new_constraints_t *con, new_constraints_t *obj,
                                    int depth_look_ahead);

2. Update approximate_state_sampler.c:
   - Add #include "solver_ctx.h"
   - In init_approximete_state:
     * Add solver_ctx_t *ctx parameter
     * Change: BranchingStats.bias = bias;
       to: ctx->branching_stats.bias = bias;
   - In CSearch_opt_sampler:
     * Add solver_ctx_t *ctx parameter
     * Find BranchingFunction call, change &BranchingStats to &ctx->branching_stats

PART B - Branching.c leaf functions (StateProbability, updated):

3. Update Branching.h:
   - Add forward declaration if needed: struct solver_ctx; typedef struct solver_ctx solver_ctx_t;
   - Or add #include "solver_ctx.h" (be careful of circular includes - forward decl is safer)
   - Update StateProbability signature:
     Before: double StateProbability(state_t *state, state_t *threshold);
     After: double StateProbability(solver_ctx_t *ctx, state_t *state, state_t *threshold);
   - Update updated() signature:
     Before: state_t *updated(state_t *bnb, size_t number_states, size_t *new_number, state_t *threshold, int sense);
     After: state_t *updated(solver_ctx_t *ctx, state_t *bnb, size_t number_states, size_t *new_number, state_t *threshold, int sense);

4. Update Branching.c:
   - Add #include "solver_ctx.h"
   - Update StateProbability:
     * Add solver_ctx_t *ctx parameter
     * Find BranchingFunction call, change &BranchingStats to &ctx->branching_stats
   - Update updated():
     * Add solver_ctx_t *ctx parameter
     * Update call to StateProbability: StateProbability(ctx, &up[a], threshold)

5. KEEP the global BranchingStats and global setter functions for backward compatibility.
   Add deprecation comment to global setters:
   /* DEPRECATED: Use solver_ctx_set_* functions instead. Will be removed in future version. */

6. Verify no remaining references to &BranchingStats in migrated functions (grep)
  </action>
  <verify>grep -n "BranchingStats" cbqs/src/approximate_state_sampler.c cbqs/src/Branching.c shows only global definition and deprecated setters, not in migrated functions</verify>
  <done>All leaf functions (approximate_state_sampler, StateProbability, updated) use ctx->branching_stats</done>
</task>

</tasks>

<verification>
- [ ] solver.h includes solver_ctx.h
- [ ] All CSearch_* functions have solver_ctx_t *ctx as first parameter
- [ ] All BranchingFunction calls in solver.c use &ctx->branching_stats
- [ ] approximate_state_sampler.h includes solver_ctx.h
- [ ] init_approximete_state and CSearch_opt_sampler have ctx parameter
- [ ] StateProbability and updated() have ctx parameter in Branching.h
- [ ] StateProbability uses ctx->branching_stats in Branching.c
- [ ] updated() passes ctx to StateProbability
- [ ] Global BranchingStats and setters remain for backward compatibility (marked deprecated)
- [ ] grep -r "BranchingStats" in migrated functions returns nothing (only global def and deprecated setters)
</verification>

<success_criteria>
1. All leaf solver functions have ctx as first parameter
2. No direct references to global BranchingStats in CSearch_*, StateProbability, updated()
3. Code compiles (will have errors until callers are updated, but syntax should be valid)
4. Backward compatibility maintained for global setters (deprecated)
</success_criteria>

<output>
After completion, create `.planning/phases/03-solver-context-architecture/03-02-SUMMARY.md`
</output>
