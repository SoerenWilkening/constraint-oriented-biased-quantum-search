---
phase: 01-test-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/test_solver.c
  - tests/test_searchlib.c
  - tests/test_integration.c
  - tests/CMakeLists.txt
autonomous: true

must_haves:
  truths:
    - "test_solver validates initial_state_preparation and update_potentials"
    - "test_searchlib validates compare function and incumbents management"
    - "Integration test solves a small knapsack and verifies returned solution satisfies all constraints"
    - "All new tests pass alongside existing tests under CTest"
  artifacts:
    - path: "tests/test_solver.c"
      provides: "CMocka tests for solver.h/c"
      contains: "cmocka_run_group_tests"
    - path: "tests/test_searchlib.c"
      provides: "CMocka tests for SearchLib.h/c"
      contains: "cmocka_run_group_tests"
    - path: "tests/test_integration.c"
      provides: "CMocka integration test solving a small problem end-to-end"
      contains: "cmocka_run_group_tests"
  key_links:
    - from: "tests/test_integration.c"
      to: "cbqs/src/solver.c"
      via: "Calls initial_state_preparation through model_t"
      pattern: "initial_state_preparation|eval_constraints"
---

<objective>
Write CMocka tests for Priority 3 modules (solver, SearchLib) and an end-to-end integration test that solves a small known problem and verifies solution feasibility. This completes the C test coverage.

Purpose: Validate the solver pipeline from state preparation through search, and prove the full system produces correct results on a known problem.
Output: tests/test_solver.c, tests/test_searchlib.c, tests/test_integration.c -- all compiling and passing.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-foundation/01-CONTEXT.md
@.planning/phases/01-test-foundation/01-RESEARCH.md
@.planning/phases/01-test-foundation/01-01-SUMMARY.md
@cbqs/src/solver.h
@cbqs/src/solver.c
@cbqs/src/SearchLib.h
@cbqs/src/SearchLib.c
@cbqs/src/local_search.h
@cbqs/src/local_search.c
@cbqs/src/quantum_search.h
@cbqs/src/model.h
@cbqs/src/constraint.h
@cbqs/src/definitions.h
@cbqs/src/test.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CMocka tests for solver and SearchLib modules</name>
  <files>tests/test_solver.c, tests/test_searchlib.c</files>
  <action>
**test_solver.c** -- Test solver state preparation and potentials:
- Include order: `stdarg.h`, `stddef.h`, `setjmp.h`, `cmocka.h`, then `solver.h`, `model.h`, `constraint.h`, `Expression.h`, `Branching.h`, `definitions.h`.
- **Setup**: Reset BranchingStats. Build a small model (3 variables, 1 constraint `x0 + x1 + x2 <= 2`, objective `-(x0 + x1 + x2)`).
- `test_initial_state_preparation`: Create a model_t, populate it with obj, con, initial_state (all zeros, n=3). Set `mod->n = 3`, `mod->M = 10`, `mod->depth_look_ahead = 0`, `mod->stopping_time = 100`, `mod->solver = OPTIMIZE`. Call `initial_state_preparation(mod)`. Verify the model's initial_state has been modified (branching assigned bits). This is a smoke test -- verify it returns without crashing and the state vector has correct bit count.
- `test_update_potentials`: Create a small constraint set. Allocate `potentials` array. Call `update_potentials(con, potentials, POSITIVE, &ret_total)`. Verify potentials are populated and `ret_total >= 0`.
- Free everything properly.

**test_searchlib.c** -- Test utility functions and incumbents:
- Include order: `stdarg.h`, `stddef.h`, `setjmp.h`, `cmocka.h`, then `SearchLib.h`, `definitions.h`.
- `test_compare_minimize`: `compare(5, 10, MINIMIZE)` should return 1 (5 < 10, so 5 is better for minimization). `compare(10, 5, MINIMIZE)` returns 0.
- `test_compare_maximize`: `compare(10, 5, MAXIMIZE)` returns 1. `compare(5, 10, MAXIMIZE)` returns 0.
- `test_compare_equal`: `compare(5, 5, MINIMIZE)` returns 0 (not strictly better).
- `test_init_incumbents`: Create initial state (n=4). `incumbents_t *inc = init_incumbents(4, initial)`. Verify `inc != NULL`, `inc->num_states >= 1`. Free with `free_incumbents`.
- `test_incumbents_initial_state`: After init, verify `inc->states[0]` matches the initial state vector.
- Free all states and incumbents.
  </action>
  <verify>
```
cd build-tests && cmake ../tests && make && ctest --output-on-failure -R "test_solver|test_searchlib"
```
All solver and searchlib tests pass.
  </verify>
  <done>test_solver.c has at least 2 test cases (initial_state_preparation smoke test, update_potentials). test_searchlib.c has at least 5 test cases (compare variants, incumbents). All pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write integration test and register all new tests in CMake</name>
  <files>tests/test_integration.c, tests/CMakeLists.txt</files>
  <action>
**test_integration.c** -- End-to-end test solving a small known-optimal problem:
- Include order: `stdarg.h`, `stddef.h`, `setjmp.h`, `cmocka.h`, then all required headers.
- **Problem**: 5-variable 0-1 knapsack (same as the existing test.c):
  - Variables: x0..x4 (binary)
  - Constraint: 2*x0 + 2*x1 + 2*x2 + 2*x3 + 2*x4 <= 33 (all items fit, trivially satisfied)
  - Objective: maximize 2*x0 + 2*x1 + 2*x2 + 2*x3 + 2*x4 (i.e., minimize the negative)
  - Known optimal: all variables = 1, objective = 10 (or -10 for minimization form)

- `test_knapsack_feasibility`: Build the knapsack model programmatically (using the expression-building pattern from test.c -- but using the safe per-term-expression pattern from research). Set up model_t with all fields. Call `initial_state_preparation(mod)` to get a state. Then verify:
  1. The returned state satisfies the constraint: `eval_constraints(&con, mod->initial_state, 5)` returns feasible.
  2. This is a feasibility-only check (per CONTEXT.md: "Integration tests verify feasibility only -- do not check objective quality since solver is heuristic").

- `test_constraint_satisfaction_after_solve`: Build a tighter knapsack: 3*x0 + 3*x1 + 3*x2 <= 6 (at most 2 items). Create initial state all-zero. Run `initial_state_preparation`. Verify the solution satisfies the constraint.

- **Known-bug expected failure pattern**: If any test reveals a known bug (e.g., BranchingStats global pollution), wrap it in a comment explaining the expected behavior and mark it with `// KNOWN_BUG: <description>` but still run it. If it fails due to a known issue, skip it with `skip()` from CMocka and a comment. Phase 2 will fix these.

**Update tests/CMakeLists.txt**: Add `add_cmocka_test` calls for test_solver, test_searchlib, test_integration.

IMPORTANT: The integration test touches the global BranchingStats. Reset it in setup. Also, `initial_state_preparation` uses `rand()` -- call `srand(42)` for deterministic behavior in tests.
  </action>
  <verify>
```
cd build-tests && cmake ../tests && make && ctest --output-on-failure
```
All 9 test executables pass (6 from Plans 01+02, 3 new). Integration test specifically verifies constraint satisfaction.
  </verify>
  <done>test_integration.c has at least 2 test cases verifying feasibility of solved knapsack problems. CMakeLists.txt registers all 9 test executables. Full CTest suite passes.</done>
</task>

</tasks>

<verification>
1. `cd build-tests && cmake ../tests && make` compiles all 9 test executables
2. `ctest --output-on-failure` reports 9/9 passing
3. Integration test solves a knapsack and verifies constraint satisfaction
4. All tests pass under ASan (`-DASAN=ON`)
5. No global state pollution between test executables (each resets BranchingStats)
</verification>

<success_criteria>
- tests/test_solver.c, tests/test_searchlib.c, tests/test_integration.c exist and compile
- All 9 C test executables pass under CTest
- Integration test verifies a solved solution satisfies constraints
- ASan clean on all tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-foundation/01-03-SUMMARY.md`
</output>
