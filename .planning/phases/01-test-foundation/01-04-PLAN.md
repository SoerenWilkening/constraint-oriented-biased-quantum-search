---
phase: 01-test-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_expression_py.py
  - tests/test_constraint_py.py
  - tests/test_model_py.py
autonomous: true

must_haves:
  truths:
    - "pytest discovers and runs Python tests from tests/ directory"
    - "Expression tests validate arithmetic operator overloading and expression building through Cython bindings"
    - "Constraint tests validate constraint creation and wrapping through Python API"
    - "Model tests validate full Model API: variable creation, constraint addition, and solve execution"
    - "Known-bug tests are marked with @pytest.mark.xfail and appear in output without breaking the suite"
  artifacts:
    - path: "tests/conftest.py"
      provides: "pytest configuration and shared fixtures"
      contains: "pytest"
    - path: "tests/test_expression_py.py"
      provides: "pytest tests for Expression Cython bindings"
      contains: "def test_"
    - path: "tests/test_constraint_py.py"
      provides: "pytest tests for Constraint Cython bindings"
      contains: "def test_"
    - path: "tests/test_model_py.py"
      provides: "pytest tests for Model API including solve"
      contains: "def test_"
  key_links:
    - from: "tests/test_model_py.py"
      to: "cbqs/Model.pyx"
      via: "imports cbqs.Model and exercises full solve pipeline"
      pattern: "from cbqs|import cbqs"
---

<objective>
Write a pytest-based Python test suite that validates the Cython bindings and Model API. This tests the full Python-facing interface that researchers actually use, complementing the C-level CMocka tests.

Purpose: Ensure the Python API (Expression building, Constraint creation, Model solve) works correctly through the Cython layer, catching binding-level bugs.
Output: tests/conftest.py, tests/test_expression_py.py, tests/test_constraint_py.py, tests/test_model_py.py -- all discoverable by pytest.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-foundation/01-CONTEXT.md
@.planning/phases/01-test-foundation/01-RESEARCH.md
@cbqs/Expression.pyx
@cbqs/Expression.pxd
@cbqs/Constraint.pyx
@cbqs/Constraint.pxd
@cbqs/Model.pyx
@cbqs/Model.pxd
@cbqs/Constants.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pytest conftest and Expression/Constraint tests</name>
  <files>tests/conftest.py, tests/test_expression_py.py, tests/test_constraint_py.py</files>
  <action>
First, read the Cython `.pyx` files to understand the Python-facing API:
- `cbqs/Expression.pyx` -- understand Expression class methods, Variable class, operator overloads
- `cbqs/Constraint.pyx` -- understand Constraint wrapper
- `cbqs/Constants.py` -- understand constant definitions (LOWER, GREATER, EQUAL, MINIMIZE, MAXIMIZE)

**tests/conftest.py**:
- Import pytest.
- Add a fixture `simple_model` that creates a fresh Model instance (if needed by multiple tests).
- Add any shared utilities.
- Ensure cbqs is importable (the package must be installed via `pip install -e .` before tests run -- document this in a comment).

**tests/test_expression_py.py** -- Test Expression Cython bindings:
- `test_variable_creation`: Import Variable from cbqs.Expression (or however the API exposes it). Create `x = Variable(0)`. Verify it's an Expression-like object.
- `test_expression_add_constant`: `x = Variable(0); expr = x + 5`. Verify expr is an Expression.
- `test_expression_add_variables`: `x0 = Variable(0); x1 = Variable(1); expr = x0 + x1`. Verify expression contains both variables.
- `test_expression_multiply_constant`: `x = Variable(0); expr = 3 * x`. Verify the coefficient is applied.
- `test_expression_subtract`: `x0 = Variable(0); x1 = Variable(1); expr = x0 - x1`. Verify subtraction works.
- `test_expression_compound`: `x0 = Variable(0); x1 = Variable(1); expr = 2*x0 + 3*x1 + 5`. Verify compound expression building.
- `test_expression_multiply_expressions`: `x0 = Variable(0); x1 = Variable(1); expr = x0 * x1`. Verify quadratic term creation.
- **Known-bug xfail test**: `@pytest.mark.xfail(reason="Integer variable expression mutation bug - Phase 2 fix")` -- `test_expression_reuse_variable`: Create `x = Variable(0); expr1 = x + 3; expr2 = x + 5`. Verify `expr1` is unchanged after creating `expr2`. (This tests the known mutation bug.)

**tests/test_constraint_py.py** -- Test Constraint Cython bindings:
- `test_constraint_creation_leq`: `x0 = Variable(0); x1 = Variable(1); c = (2*x0 + 3*x1 <= 10)`. Verify c is a Constraint object with correct sense.
- `test_constraint_creation_geq`: `(x0 + x1 >= 1)`. Verify GREATER sense.
- `test_constraint_creation_eq`: `(x0 + x1 == 2)`. Verify EQUAL sense.
- Adapt test cases to match the actual Constraint API discovered from reading the .pyx files. The constraint may be created by calling methods on Expression rather than using Python comparison operators -- adjust accordingly.
  </action>
  <verify>
```
cd /path/to/project && pip install -e . && pytest tests/test_expression_py.py tests/test_constraint_py.py -v
```
All tests pass (xfail tests shown as expected failures in output).
  </verify>
  <done>conftest.py exists. test_expression_py.py has at least 8 tests (including 1 xfail). test_constraint_py.py has at least 3 tests. pytest reports all passing (xfails shown as xfail, not failure).</done>
</task>

<task type="auto">
  <name>Task 2: Write Model API pytest tests including solve integration</name>
  <files>tests/test_model_py.py</files>
  <action>
First, read `cbqs/Model.pyx` to understand the full Model API: how variables are added, how constraints and objectives are set, and how solve is called.

**tests/test_model_py.py** -- Test the user-facing Model API:
- `test_model_creation`: Create `Model()` instance. Verify it exists and has expected initial state.
- `test_add_binary_variable`: `m = Model(); x = m.addVar()` (or however the API works). Verify variable is returned.
- `test_add_multiple_variables`: Add several variables. Verify they have distinct indices.
- `test_set_objective`: Create model with variables, set objective expression. No crash.
- `test_add_constraint`: Create model, add a constraint. No crash.
- `test_solve_small_knapsack`: Build a 5-variable knapsack (matching the C test.c example):
  - Variables: x0..x4
  - Constraint: 2*x0 + 2*x1 + 2*x2 + 2*x3 + 2*x4 <= 33
  - Objective: maximize sum
  - Call solve/optimize.
  - Verify: the returned solution satisfies the constraint (feasibility check only -- do NOT check objective optimality since solver is heuristic).
- `test_solve_tight_knapsack`: Build a tighter knapsack: items with weights [3, 4, 5, 2], capacity 8. Solve. Verify constraint satisfaction.
- `test_solve_satisfaction_problem`: If the API supports satisfaction mode (SATISFY), create a constraint satisfaction problem and verify feasibility.

Adapt all tests to the actual API discovered from reading Model.pyx. The API might use methods like `m.addVar()`, `m.addConstr()`, `m.setObjective()`, `m.optimize()` -- or different names. Match what exists.
  </action>
  <verify>
```
pytest tests/test_model_py.py -v
```
All tests pass. Solve tests complete within reasonable time (< 30 seconds each).
  </verify>
  <done>test_model_py.py has at least 7 tests covering model creation, variable/constraint/objective setup, and solve with feasibility verification. All pass.</done>
</task>

</tasks>

<verification>
1. `pytest tests/ -v` discovers and runs all Python tests
2. All tests pass (xfail tests shown as expected failures)
3. Solve integration tests complete within 30 seconds
4. No import errors (cbqs package is installed)
</verification>

<success_criteria>
- tests/conftest.py, test_expression_py.py, test_constraint_py.py, test_model_py.py exist
- `pytest tests/ -v` runs all Python tests successfully
- At least 1 xfail test for the known expression mutation bug
- Solve tests verify constraint satisfaction (feasibility only)
- Total Python test count >= 18
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-foundation/01-04-SUMMARY.md`
</output>
