---
phase: 16-global-state-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cbqs/src/Branching.h
  - cbqs/src/Branching.c
  - cbqs/src/solver_ctx.h
  - cbqs/src/solver_ctx.c
  - tests/test_solver.c
  - tests/test_local_search.c
  - tests/test_integration.c
autonomous: true

must_haves:
  truths:
    - "No global or file-scope BranchingStats_t variable exists in Branching.h or Branching.c"
    - "The C functions set_factors(), set_bias(), set_obj_dependence(), set_constraint_dependence() do not exist in any .c or .h file"
    - "All C tests pass (make test) with zero failures"
    - "No linker errors referencing BranchingStats symbol"
  artifacts:
    - path: "cbqs/src/Branching.h"
      provides: "BranchingStats_t struct and BranchingFunction inline (no global extern)"
      contains: "typedef struct"
    - path: "cbqs/src/Branching.c"
      provides: "StateProbability and updated functions (no global definition)"
      contains: "StateProbability"
    - path: "tests/test_solver.c"
      provides: "Solver tests without global BranchingStats reset"
    - path: "tests/test_local_search.c"
      provides: "Local search tests without reset_branching_stats function"
    - path: "tests/test_integration.c"
      provides: "Integration tests without reset_branching_stats function"
  key_links:
    - from: "tests/test_solver.c"
      to: "cbqs/src/Branching.h"
      via: "#include Branching.h (for BranchingStats_t type only, not global)"
      pattern: '#include "Branching.h"'
    - from: "cbqs/src/Branching.c"
      to: "cbqs/src/solver_ctx.h"
      via: "ctx->branching_stats usage in StateProbability"
      pattern: "ctx->branching_stats"
---

<objective>
Remove the dead global BranchingStats variable from the C layer and update all C test files that reference it.

Purpose: GLOB-01 (no global BranchingStats) and GLOB-02 verification (no deprecated C setter functions). The global variable is dead code -- all kernel code uses ctx->branching_stats. Three test files still reset the global for anti-pollution, but they already create fresh solver_ctx_t per test, making the resets unnecessary.

Output: Clean C layer with no global BranchingStats symbol, all C tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-global-state-removal/16-RESEARCH.md
@cbqs/src/Branching.h
@cbqs/src/Branching.c
@cbqs/src/solver_ctx.h
@cbqs/src/solver_ctx.c
@tests/test_solver.c
@tests/test_local_search.c
@tests/test_integration.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove global BranchingStats from C headers and sources</name>
  <files>
    cbqs/src/Branching.h
    cbqs/src/Branching.c
    cbqs/src/solver_ctx.h
    cbqs/src/solver_ctx.c
  </files>
  <action>
  In `cbqs/src/Branching.h`:
  - Delete line 27: `extern BranchingStats_t BranchingStats; // branching stats as global variable`
  - Replace with a comment: `/* Global BranchingStats removed in v2.0 -- all state lives in solver_ctx_t.branching_stats */`

  In `cbqs/src/Branching.c`:
  - Delete lines 6-13 (the entire `BranchingStats_t BranchingStats = {...};` initializer block)
  - Replace with a comment: `/* Global BranchingStats removed in v2.0 -- all state lives in solver_ctx_t */`

  In `cbqs/src/solver_ctx.h`:
  - Line 6: Update the `@brief` from "replacing global variables like BranchingStats and stop_flag" to "encapsulating all per-solve mutable state (replaces former global variables)"
  - Line 75: Update comment from "branching_stats with default values (matching global BranchingStats)" to "branching_stats with default values"

  In `cbqs/src/solver_ctx.c`:
  - Line 30: Update comment from "Initialize branching_stats with same defaults as global BranchingStats" to "Initialize branching_stats with default values"

  IMPORTANT: Do NOT delete `cbqs/src/Branching.h` or `cbqs/src/Branching.c` -- only remove the global variable. The BranchingStats_t struct typedef, BranchingFunction inline, StateProbability function, and updated function all stay.
  </action>
  <verify>
  Run: `grep -rn 'BranchingStats[^_]' cbqs/src/Branching.h cbqs/src/Branching.c` -- should show only the comment lines, not any variable declaration or definition.
  Run: `grep -rn 'extern.*BranchingStats' cbqs/src/` -- should return zero matches.
  Run: `grep -rn 'set_factors\|set_bias\|set_obj_dependence\|set_constraint_dependence' cbqs/src/*.c cbqs/src/*.h` -- should return zero matches (GLOB-02 already satisfied, just verify).
  </verify>
  <done>No extern declaration or file-scope definition of BranchingStats exists in any C source or header. Comments referencing the old global are updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update C test files to remove global BranchingStats references</name>
  <files>
    tests/test_solver.c
    tests/test_local_search.c
    tests/test_integration.c
  </files>
  <action>
  In `tests/test_solver.c`:
  - Delete lines 18-24 in `build_small_model()` (the 7 lines that reset `BranchingStats.*` fields including the comment). These tests already build their own model and the solver kernel uses ctx->branching_stats, not the global.

  In `tests/test_local_search.c`:
  - Delete the `reset_branching_stats()` function definition (lines 36-43)
  - Delete its comment block (lines 33-35: "Helper: reset BranchingStats global...")
  - Delete all calls to `reset_branching_stats()` at lines 123, 167, 207, 258
  - Each test already creates a fresh `solver_ctx_t *ctx = solver_ctx_create()` which initializes its own branching_stats with defaults.

  In `tests/test_integration.c`:
  - Delete the `reset_branching_stats()` function definition (lines 17-24)
  - Delete its comment block (lines 13-16: "Helper: reset BranchingStats global...")
  - Delete all calls to `reset_branching_stats()` at lines 153, 178
  - These tests already call `srand(42)` for determinism and `initial_state_preparation()` which uses the model's own state.

  All three files still `#include "Branching.h"` -- this is correct because they need the `BranchingStats_t` type definition (used by solver_ctx_t). Do NOT remove the include.

  After editing, build and run C tests:
  ```
  cd /Users/sorenwilkening/Desktop/constraint-oriented-biased-quantum-search
  mkdir -p build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Debug && make -j$(nproc) && ctest --output-on-failure
  ```
  </action>
  <verify>
  Run: `grep -rn 'BranchingStats\.' tests/test_solver.c tests/test_local_search.c tests/test_integration.c` -- should return zero matches (no field access on the removed global).
  Run: `grep -rn 'reset_branching_stats' tests/` -- should return zero matches.
  Run C tests: `cd build && ctest --output-on-failure` -- all tests pass with zero failures.
  </verify>
  <done>All three C test files compile and pass without any reference to the removed global BranchingStats variable. No linker errors.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'extern.*BranchingStats' cbqs/src/` returns zero matches
2. `grep -rn 'BranchingStats\.' cbqs/src/Branching.c cbqs/src/Branching.h tests/test_solver.c tests/test_local_search.c tests/test_integration.c` returns zero matches (no field access on a global)
3. `grep -rn 'set_factors\|set_bias\b.*void\|set_obj_dependence\|set_constraint_dependence' cbqs/src/*.c cbqs/src/*.h` returns zero matches (GLOB-02)
4. C tests build and pass: `cd build && cmake .. && make -j$(nproc) && ctest --output-on-failure` -- 0 failures
</verification>

<success_criteria>
- GLOB-01: No global or file-scope BranchingStats_t variable in Branching.h or Branching.c
- GLOB-02: No set_factors(), set_bias(), set_obj_dependence(), set_constraint_dependence() in any .c/.h
- All C tests pass with zero failures and zero linker errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-global-state-removal/16-01-SUMMARY.md`
</output>
