---
phase: 17-test-suite-finalization
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - tests/test_cython_memory.py
autonomous: true

must_haves:
  truths:
    - "Valgrind reports zero leaks for C test_branching (covers weights lifecycle: set, overwrite, realloc, clear, free)"
    - "Valgrind reports zero leaks for Python branching_weights allocation/deallocation through solve cycles"
    - "Repeated set_param('branching_weights', ...) with changing sizes across multiple solve() calls does not leak"
  artifacts:
    - path: "tests/test_cython_memory.py"
      provides: "New Valgrind-targeted test: repeated weights reallocation across solve lifecycles"
      contains: "test_branching_weights_realloc_no_leak"
  key_links:
    - from: "tests/test_cython_memory.py"
      to: "cbqs/SearchLib.pyx"
      via: "set_param('branching_weights', ...) triggering solver_ctx_set_branching_weights in C"
      pattern: "set_param.*branching_weights"
    - from: "tests/test_branching.c"
      to: "cbqs/src/solver_ctx.c"
      via: "solver_ctx_set_branching_weights() malloc/realloc/free lifecycle"
      pattern: "solver_ctx_set_branching_weights"
---

<objective>
Run Valgrind on C and Python tests to verify zero memory leaks for branching_weights allocation, deallocation, and reallocation across solve lifecycles.

Purpose: Satisfy TEST-04 (Valgrind clean for branching_weights) from the Phase 17 success criteria.
Output: Documented Valgrind results showing zero leaks, new reallocation-focused memory test added.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-test-suite-finalization/17-RESEARCH.md
@.planning/phases/17-test-suite-finalization/17-01-SUMMARY.md

@tests/test_cython_memory.py
@tests/test_branching.c
@tests/CMakeLists.txt
@tests/valgrind-python.supp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Valgrind on C tests and add reallocation memory test</name>
  <files>tests/test_cython_memory.py</files>
  <action>
**Step 1: Run Valgrind on C test_branching:**

Build C tests in debug mode for Valgrind (no ASan — ASan conflicts with Valgrind):
```
cd tests && mkdir -p build-valgrind && cd build-valgrind
cmake .. -DCMAKE_BUILD_TYPE=Debug
cmake --build .
```

Run Valgrind on test_branching:
```
valgrind --leak-check=full --error-exitcode=1 ./test_branching
```

This covers: weights set, overwrite (same size), realloc (different sizes from Plan 17-01), clear (NULL), single-element, normalization, and solver_ctx_free cleanup.

If Valgrind reports leaks: analyze the leak trace, identify the source (should be in solver_ctx.c), and fix. Most likely fix would be in solver_ctx_free() not freeing branching_weights, or solver_ctx_set_branching_weights() not freeing old array before realloc.

**Step 2: Run Valgrind on Python memory tests:**

```
PYTHONMALLOC=malloc valgrind --leak-check=full \
    --suppressions=tests/valgrind-python.supp \
    --error-exitcode=1 \
    python -m pytest tests/test_cython_memory.py::TestBranchingMemory -v -x
```

This runs the existing `test_branching_weights_no_leak` test (20 iterations of create/set_param/solve/del).

If Valgrind reports leaks NOT covered by suppressions: check if it's the pre-existing preprocessing() leak (documented in MEMORY.md — out of scope). If so, add a targeted suppression to tests/valgrind-python.supp for it. If it's a new branching_weights leak, fix it.

**Step 3: Add reallocation lifecycle memory test to test_cython_memory.py:**

Add a new test to `TestBranchingMemory`:

```python
def test_branching_weights_realloc_no_leak(self):
    """Repeated set_param with different-sized weights across solves should not leak."""
    from cbqs.Model import Model
    from cbqs.Constants import MAXIMIZE

    for size in [5, 10, 20, 10, 5, 15, 3]:
        m = Model()
        x = m.add_variables(size)
        m.set_objective(sum(x[i] for i in range(size)), sense=MAXIMIZE)
        m.add_constraint(sum(x[i] for i in range(size)) <= size // 2)
        m.close()
        m.set_param("branching_weights", [float(i + 1) for i in range(size)])
        m.set_param("stopping_time", 1)
        m.set_param("num_workers", 1)
        m.solve()
        # Change weights to different size within same model (tests realloc)
        # This can only be done if model supports it; if not, the cross-model
        # variation above still exercises the alloc/free cycle
        del m

    gc.collect()
```

**Step 4: Run Valgrind on the new test:**

```
PYTHONMALLOC=malloc valgrind --leak-check=full \
    --suppressions=tests/valgrind-python.supp \
    --error-exitcode=1 \
    python -m pytest tests/test_cython_memory.py::TestBranchingMemory -v -x
```

Verify zero leaks for both existing and new branching memory tests.

**Step 5: Run Valgrind on additional C test targets** to confirm no regressions:

```
cd tests/build-valgrind
valgrind --leak-check=full --error-exitcode=1 ./test_solver
valgrind --leak-check=full --error-exitcode=1 ./test_thread_safety
```

These two targets also use solver_ctx_t and BranchingStats, so they verify no leaks in the broader context lifecycle.

Document all Valgrind results (pass/fail, leak counts if any).
  </action>
  <verify>
`valgrind --leak-check=full --error-exitcode=1 ./test_branching` exits with code 0 (zero leaks).
`PYTHONMALLOC=malloc valgrind --leak-check=full --suppressions=tests/valgrind-python.supp --error-exitcode=1 python -m pytest tests/test_cython_memory.py::TestBranchingMemory -v` exits with code 0 (zero leaks).
`python -m pytest tests/test_cython_memory.py -v` passes all tests including the new reallocation test.
  </verify>
  <done>Valgrind confirms zero leaks for branching_weights in both C and Python test paths. New reallocation lifecycle test added and verified under Valgrind. TEST-04 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Final full-suite verification</name>
  <files></files>
  <action>
Run the complete test suite one final time to confirm everything works together after all Phase 17 changes:

1. Full Python suite: `python -m pytest tests/ -v --tb=short`
   - Document total pass count (expect 390+: 383 original + 4 new propagation + 2 new determinism + 1 new memory)
   - Verify zero failures

2. Full C suite: `cd tests/build && cmake --build . && ctest --output-on-failure`
   - Document total pass count across all test targets
   - Verify zero failures

3. Verify TEST-01 through TEST-04 checklist:
   - TEST-01: Grep for `\.solve(` with arguments in test files — confirm zero matches (all calls are zero-arg)
   - TEST-02: Confirm new tests exist for different-weights, all-zero, large-n, combined-factors, reallocation, single-element
   - TEST-03: Confirm cross-lifecycle determinism tests exist and pass
   - TEST-04: Document Valgrind results from Task 1

If any test fails at this stage, fix it before completing.
  </action>
  <verify>
`python -m pytest tests/ -v --tb=short` reports all tests passed, 0 failed.
`cd tests/build && ctest --output-on-failure` reports all tests passed.
No `solve(` calls with keyword arguments exist in test files.
  </verify>
  <done>Full test suite passes with all new tests. TEST-01, TEST-02, TEST-03, TEST-04 all verified. Phase 17 success criteria met.</done>
</task>

</tasks>

<verification>
1. Valgrind on C test_branching: zero leaks, exit code 0
2. Valgrind on Python TestBranchingMemory: zero leaks (with PYTHONMALLOC=malloc and suppression file)
3. Full Python suite: all tests pass (390+)
4. Full C suite: all targets pass
5. TEST-01: No solve() calls with keyword arguments in any test file
6. TEST-02: New tests cover different-weights, all-zero, large-n, combined-factors, reallocation, single-element
7. TEST-03: Cross-lifecycle determinism verified
8. TEST-04: Valgrind clean for branching_weights lifecycle
</verification>

<success_criteria>
- Valgrind reports zero leaks for test_branching C tests
- Valgrind reports zero leaks for Python branching memory tests (with suppression file)
- New reallocation lifecycle test added to test_cython_memory.py and passes under Valgrind
- Full Python and C test suites pass with zero failures
- All four TEST requirements (TEST-01 through TEST-04) verified and documented
</success_criteria>

<output>
After completion, create `.planning/phases/17-test-suite-finalization/17-02-SUMMARY.md`
</output>
