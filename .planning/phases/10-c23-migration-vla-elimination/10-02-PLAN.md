---
phase: 10-c23-migration-vla-elimination
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - tests/CMakeLists.txt
  - CMakeLists.txt
  - cbqs/src/solver.c
  - cbqs/src/constraint.c
  - cbqs/src/local_search.c
  - .github/workflows/test.yml
autonomous: true

must_haves:
  truths:
    - "CMake test build uses -std=gnu17 -Wall -Wextra -Wpedantic -Wvla"
    - "tests/CMakeLists.txt has WERROR option that gates -Werror"
    - "CI cmake invocations use -DWERROR=ON"
    - "All sign comparison warnings are fixed by changing variable types (not casts)"
    - "All existing tests pass with strict warning flags enabled"
  artifacts:
    - path: "tests/CMakeLists.txt"
      provides: "Strict warning flags and WERROR option"
      contains: "Wvla"
    - path: ".github/workflows/test.yml"
      provides: "CI with -DWERROR=ON"
      contains: "WERROR"
  key_links:
    - from: "tests/CMakeLists.txt"
      to: "all C test targets"
      via: "target_compile_options"
      pattern: "-Wall.*-Wextra.*-Wpedantic"
    - from: ".github/workflows/test.yml"
      to: "tests/CMakeLists.txt"
      via: "cmake -DWERROR=ON"
      pattern: "WERROR"
---

<objective>
Enable strict compiler warning flags (-Wall -Wextra -Wpedantic -Wvla) in the test build system, fix all remaining sign/type mismatch warnings, and update CI to build with -Werror. This hardens the codebase against future regressions.

Purpose: Achieve zero-warning compilation under GCC 15 strict mode, satisfying the phase success criteria. Enable -Werror in CI only (per user decision) so local dev builds are not broken.
Output: Warning-free test build, CI configured with -DWERROR=ON, VLA regression guard via -Wvla.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-c23-migration-vla-elimination/10-CONTEXT.md
@.planning/phases/10-c23-migration-vla-elimination/10-RESEARCH.md
@.planning/phases/10-c23-migration-vla-elimination/10-01-SUMMARY.md
@tests/CMakeLists.txt
@CMakeLists.txt
@.github/workflows/test.yml
@cbqs/src/solver.c
@cbqs/src/constraint.c
@cbqs/src/local_search.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update build system with strict warning flags</name>
  <files>
    tests/CMakeLists.txt
    CMakeLists.txt
  </files>
  <action>
  **tests/CMakeLists.txt:**

  1. Change `set(CMAKE_C_STANDARD 11)` to `set(CMAKE_C_STANDARD 17)`.

  2. Add a `WERROR` CMake option after `option(ASAN ...)`:
  ```cmake
  option(WERROR "Enable -Werror for strict warning compliance" OFF)
  ```

  3. Inside the `add_cmocka_test` function, after the existing `if(ASAN)` block, add warning flags applied to ALL test targets:
  ```cmake
  # Strict warning flags for C23-forward compatibility
  target_compile_options(${NAME} PRIVATE
      -Wall -Wextra -Wpedantic -Wvla
      -Wno-unused-parameter  # CMocka test helpers have unused params
  )
  if(WERROR)
      target_compile_options(${NAME} PRIVATE -Werror)
  endif()
  ```

  Note: `-Wno-unused-parameter` is needed because CMocka test functions use `void **state` parameter that is often unused. This is more practical than annotating every test function with CBQS_UNUSED.

  **CMakeLists.txt (root):**
  1. Change `set(CMAKE_C_STANDARD 11)` to `set(CMAKE_C_STANDARD 17)`.
  2. The root CMakeLists.txt builds the main executable (test.c). Add after the existing `add_executable` block:
  ```cmake
  target_compile_options(constraint-oriented-biased-quantum-search PRIVATE
      -Wall -Wextra -Wpedantic -Wvla
  )
  ```

  Do NOT add -Werror to the root CMakeLists.txt (user decision: -Werror in CI only).
  </action>
  <verify>
  Build with warnings enabled:
  ```
  cd /Users/sorenwilkening/Desktop/constraint-oriented-biased-quantum-search
  rm -rf build-warnings
  mkdir build-warnings && cd build-warnings
  cmake ../tests -DWERROR=OFF 2>&1 | tail -5
  make -j$(nproc) 2>&1 | grep -c "warning:"
  ```
  Note the warning count -- Task 2 will fix these.
  </verify>
  <done>Build system uses C17 standard with -Wall -Wextra -Wpedantic -Wvla flags, WERROR option exists</done>
</task>

<task type="auto">
  <name>Task 2: Fix all type mismatch and sign comparison warnings</name>
  <files>
    cbqs/src/solver.c
    cbqs/src/constraint.c
    cbqs/src/local_search.c
  </files>
  <action>
  Build with `-DWERROR=ON` to surface all warnings. Fix each warning by changing VARIABLE TYPES to match signedness (per user decision: no casts).

  **Expected warnings and fixes:**

  **cbqs/src/local_search.c:**
  - `int C = dat->con->num_constraints;` at lines ~178, ~322, ~484, ~569: Change `int C` to `uint32_t C` since `num_constraints` is `uint32_t`. This fixes signed/unsigned comparison warnings in all loops like `for (int i = 0; i < C; ++i)` -- change loop vars to `uint32_t` where they iterate against C.
  - Line 545: `if (time > mod->stopping_time || (cur_sol->tot_profit <= mod->stop_val) && (mod->stop_val != -1)) break;` -- Add parentheses around the `||` and `&&` operands to fix `-Wparentheses`: `if (time > mod->stopping_time || ((cur_sol->tot_profit <= mod->stop_val) && (mod->stop_val != -1))) break;`

  **cbqs/src/solver.c:**
  - Multiple functions use `size_t C = con->num_constraints;` then loop with `for (int cnstr = 0; cnstr < C; cnstr++)`. Change `int cnstr` to `size_t cnstr` or `uint32_t cnstr` to match C's type. Alternatively if C is used as `size_t`, change loop vars to `size_t`.
  - Functions `evaluation`, `update_potentials`, `adjusted_constraint_violation` in solver.c and constraint.c: same pattern -- loop variables `int cnstr` iterating against `size_t C`.
  - Lines with `count[0] > 0 && count[1] > 0 || count[0] == 0 && count[1] == 0` and similar: add parentheses for `-Wparentheses`.
  - Lines like `if (negative == POSITIVE && is_closed || negative == NEGATIVE)` (line ~36): add parentheses: `if ((negative == POSITIVE && is_closed) || negative == NEGATIVE)`.
  - Unused variable `all_positive` (declared but never used) in CSearch_opt at ~line 332, and in CSearch_opt_monte_carlo_sampler at ~line 710: remove the declaration.
  - Unused variable `both_infeasible` in CSearch_opt_sat_monte_carlo_sampler: remove or mark with CBQS_UNUSED if it's read later.

  **cbqs/src/constraint.c:**
  - In `print_new_constraint`: lines that use `%zu` format specifier for `uint32_t` variables (e.g., `con->variables[...]` printed as `%zu`). Change to `%u` or `%" PRIu32 "` from `<inttypes.h>`.
  - In `adjusted_constraint_violation`: `for (int cnstr = 0; cnstr < C; cnstr++)` where C is `size_t`. Change `int cnstr` to `size_t cnstr`.
  - In `objective_value_improved`: loop vars iterating against uint32_t fields -- fix types.

  **Strategy:**
  1. Build with `-DWERROR=ON` and capture ALL warnings
  2. Fix each warning by changing the variable declaration type (not adding casts)
  3. Re-build until zero warnings
  4. Run tests to verify no behavioral regressions

  **Important:** When changing `int` loop variables to `uint32_t`, verify the loop doesn't depend on negative values (e.g., `i >= 0` in a decrementing loop with unsigned type would be an infinite loop). In this codebase, all such loops increment from 0 upward, so the change is safe.
  </action>
  <verify>
  Build with -Werror:
  ```
  cd /Users/sorenwilkening/Desktop/constraint-oriented-biased-quantum-search
  rm -rf build-werror
  mkdir build-werror && cd build-werror
  cmake ../tests -DWERROR=ON
  make -j$(nproc) 2>&1
  ```
  Must compile with ZERO warnings/errors.

  Run full test suite:
  ```
  cd build-werror && ctest --output-on-failure
  ```
  All tests must pass.
  </verify>
  <done>Zero warnings with -Wall -Wextra -Wpedantic -Wvla -Werror, all tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Update CI to enforce -Werror and verify Cython compatibility</name>
  <files>
    .github/workflows/test.yml
    cbqs/SearchLib.pxd
  </files>
  <action>
  **`.github/workflows/test.yml`:**

  In the `c-tests` job, change the Configure step from:
  ```yaml
  run: mkdir build-tests && cd build-tests && cmake ../tests
  ```
  to:
  ```yaml
  run: mkdir build-tests && cd build-tests && cmake ../tests -DWERROR=ON
  ```

  In the `c-tests-asan` job, change the Configure step from:
  ```yaml
  run: mkdir build-tests-asan && cd build-tests-asan && cmake ../tests -DASAN=ON
  ```
  to:
  ```yaml
  run: mkdir build-tests-asan && cd build-tests-asan && cmake ../tests -DASAN=ON -DWERROR=ON
  ```

  In the `c-tests-valgrind` job, change the Configure step from:
  ```yaml
  run: mkdir build-tests-valgrind && cd build-tests-valgrind && cmake ../tests
  ```
  to:
  ```yaml
  run: mkdir build-tests-valgrind && cd build-tests-valgrind && cmake ../tests -DWERROR=ON
  ```

  Do NOT change the `c-tests-tsan` job -- it uses Clang with custom CMAKE_C_FLAGS and adding -Werror there may require separate Clang-specific warning suppressions. Leave for future phase if needed.

  **`cbqs/SearchLib.pxd`:**
  Verify that the Cython `.pxd` declaration `ctypedef void (*callback_t)()` still works after the C-side change to `(void)`. In Cython, `()` in a C function type means "unspecified parameters" and is compatible with `(void)` in C. No change needed to the .pxd file -- just verify the Python package still builds:
  ```
  cd /Users/sorenwilkening/Desktop/constraint-oriented-biased-quantum-search
  pip install --no-build-isolation -e . 2>&1 | tail -10
  ```

  If the Cython build produces warnings about callback_t, suppress them with a pragma in the generated C file (but this is unlikely -- Cython handles this correctly).
  </action>
  <verify>
  1. Read `.github/workflows/test.yml` and verify all three standard C test jobs use `-DWERROR=ON`
  2. Build Python package: `pip install --no-build-isolation -e .` completes successfully
  3. Run Python tests: `pytest tests/ -x -q --ignore=tests/test_stress.py` -- all pass
  4. Final C build verification:
  ```
  cd /Users/sorenwilkening/Desktop/constraint-oriented-biased-quantum-search
  rm -rf build-final
  mkdir build-final && cd build-final
  cmake ../tests -DWERROR=ON
  make -j$(nproc) 2>&1
  ctest --output-on-failure
  ```
  </verify>
  <done>CI enforces -Werror for C tests, Python/Cython package builds cleanly, all tests pass</done>
</task>

</tasks>

<verification>
After all tasks:
1. `cmake ../tests -DWERROR=ON && make` compiles with zero warnings
2. `ctest --output-on-failure` -- all C tests pass
3. `pip install --no-build-isolation -e .` -- Cython build succeeds
4. `pytest tests/ -x -q --ignore=tests/test_stress.py` -- all Python tests pass
5. `.github/workflows/test.yml` contains `-DWERROR=ON` in c-tests, c-tests-asan, c-tests-valgrind jobs
6. `grep -rn 'CMAKE_C_STANDARD 17' tests/CMakeLists.txt CMakeLists.txt` -- both show C17
7. `grep -rn '\-Wvla' tests/CMakeLists.txt` -- VLA regression guard present
</verification>

<success_criteria>
- Zero warnings with GCC using -Wall -Wextra -Wpedantic -Wvla -Werror
- All sign comparison warnings fixed by type changes (no casts)
- CI enforces -Werror on standard, ASan, and Valgrind builds
- VLA regression guard (-Wvla) prevents future VLA introduction
- C standard updated to C17 (gnu17) in both CMakeLists.txt files
- Cython build unaffected by C-side changes
- Full test suite (C + Python) passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-c23-migration-vla-elimination/10-02-SUMMARY.md`
</output>
