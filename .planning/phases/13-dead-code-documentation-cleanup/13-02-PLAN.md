---
phase: 13-dead-code-documentation-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cbqs/Model.pyx
  - cbqs/Expression.pyx
  - cbqs/state.pyx
  - cbqs/Constraint.pyx
  - cbqs/state_sampler.pyx
  - cbqs/SearchLib.pyx
  - setup.py
  - CMakeLists.txt
autonomous: true

must_haves:
  truths:
    - "No commented-out debug print statements remain in any .pyx file"
    - "No unguarded print() calls exist in Model.pyx production code (approximate_benchmarking, solution property)"
    - "No bare except clauses exist in any production .pyx file"
    - "Dead code in setup.py (sources_circuit) is removed"
    - "Stale include path (iqs/src) removed from root CMakeLists.txt"
    - "Full test suite passes with zero behavioral changes"
  artifacts:
    - path: "cbqs/Model.pyx"
      provides: "Clean Model with no dead code or unguarded prints"
    - path: "cbqs/Expression.pyx"
      provides: "Clean Expression with no commented-out code"
    - path: "setup.py"
      provides: "Clean build config with no unused source lists"
  key_links:
    - from: "cbqs/Model.pyx"
      to: "tests/"
      via: "pytest"
      pattern: "all tests pass"
---

<objective>
Remove all dead code from the Cython/Python layer and clean up build configuration files.

Purpose: Eliminate commented-out code, unguarded print statements, and dead build configuration from the Cython middleware and build system. Verify that the bare except clause issue (CLEAN-03) is already resolved. This completes the full codebase sweep alongside Plan 01 (which handles the C kernel).

Output: Clean .pyx files with no dead code, clean setup.py and CMakeLists.txt with no stale references.
</objective>

<execution_context>
@/home/agent/.claude/get-shit-done/workflows/execute-plan.md
@/home/agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dead-code-documentation-cleanup/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead code and unguarded prints from all Cython files</name>
  <files>
    cbqs/Model.pyx
    cbqs/Expression.pyx
    cbqs/state.pyx
    cbqs/Constraint.pyx
    cbqs/state_sampler.pyx
    cbqs/SearchLib.pyx
  </files>
  <action>
    Remove all commented-out code and unguarded debug print statements from production .pyx files. Use the inventory from 13-RESEARCH.md. Also verify CLEAN-03 (bare except clauses).

    **Model.pyx** -- Remove ALL of these:
    - Line ~99: commented-out gpu_executor type hint
    - Line ~118: commented-out runtime
    - Line ~122: commented-out objective_value
    - Lines ~197, 284-286: commented-out print/code blocks
    - Line ~279: commented-out initial_state line
    - Lines ~306-307: commented-out sparsity/print lines
    - Line ~498: UNGUARDED `print(state)` in approximate_benchmarking -- REMOVE (debug noise in production method)
    - Lines ~539-542: UNGUARDED print statements in solution property -- REMOVE (debug noise)

    **Expression.pyx** -- Remove ALL of these:
    - Lines ~182-183: commented-out print_expression calls in mul_expr
    - Lines ~370-378: commented-out potential calculation in __eq__
    - NOTE: Lines ~150-151 are print statements in __str__ -- these are INTENTIONAL (they implement __str__). DO NOT remove.

    **state.pyx** -- Remove ALL of these:
    - Lines ~49-50: commented-out __del__
    - Lines ~90, 93-94, 96-97, 98, 101, 105-106: commented-out print/debug lines

    **Constraint.pyx** -- Remove ALL of these:
    - Lines ~1-2: commented-out function wrapper
    - Lines ~12, 54: commented-out print statements

    **state_sampler.pyx** -- Remove:
    - Line ~3: commented-out import

    **SearchLib.pyx** -- Remove:
    - Lines ~321-323: commented-out monte carlo estimation block

    **Bare except verification (CLEAN-03):**
    After removals, run: `grep -rn "except:" cbqs/*.pyx | grep -v "except " | grep -v "except:"` to confirm no bare excepts exist. Per RESEARCH.md, this is already satisfied -- all except clauses specify `Exception`. Just verify and confirm.

    If any bare `except:` clauses ARE found in production .pyx files (not test files), replace them with the narrowest appropriate exception type:
    - For callback wrappers: `except Exception:`
    - For attribute access: `except (AttributeError, TypeError):`
    - For numeric operations: `except (ValueError, TypeError):`

    Leave test file bare excepts alone per user decision.

    After all removals, verify compilation:
    ```bash
    pip install -e . 2>&1 | tail -20
    ```
  </action>
  <verify>
    Run: `pip install -e .` completes without errors.
    Run: `grep -rn "^[[:space:]]*#.*print\|^[[:space:]]*#.*printf" cbqs/Model.pyx cbqs/Expression.pyx cbqs/state.pyx cbqs/Constraint.pyx cbqs/state_sampler.pyx cbqs/SearchLib.pyx | wc -l` returns 0 or near-0 (no commented-out print statements).
    Run: `grep -n "except:" cbqs/*.pyx` returns only `except Exception:` or `except SomeType:` lines (no bare excepts).
  </verify>
  <done>
    All commented-out code removed from 6 .pyx files. Unguarded print() calls removed from Model.pyx approximate_benchmarking and solution property. CLEAN-03 verified (no bare except clauses in production .pyx files). Project compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean up build configuration and run full test suite</name>
  <files>
    setup.py
    CMakeLists.txt
  </files>
  <action>
    Clean up dead code in build configuration files and run the full test suite to verify all Phase 13 Plan 02 changes.

    **setup.py:**
    - Remove the `sources_circuit` list variable (lines ~16-35). This defines source files for a CircuitBackendBinder extension that is never added to the `extensions` list. Dead code in build config.
    - Do NOT remove the `pandas` dependency yet -- it may be used by user scripts. Flag it with a comment: `# TODO: verify pandas is still needed`
    - Do NOT remove CircuitBackendBinder import in __init__.py (it has try/except for graceful failure already).

    **Root CMakeLists.txt:**
    - Remove the stale `iqs/src` include directory reference. The `iqs/src` directory does not exist in the project tree.
    - Leave the `metal_test` target as-is (it is platform-specific for macOS, not stale).
    - Leave the main executable target using `test.c` as-is (it may be used for quick manual testing).

    After all changes, run the full test suite:
    ```bash
    cd /Users/sorenwilkening/Desktop/constraint-oriented-biased-quantum-search
    pip install -e . && python -m pytest tests/ -x -q
    ```

    Also run C tests:
    ```bash
    cd /Users/sorenwilkening/Desktop/constraint-oriented-biased-quantum-search
    mkdir -p build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Debug && make -j$(nproc) && ctest --output-on-failure
    ```
  </action>
  <verify>
    Run: `pip install -e . && python -m pytest tests/ -x -q` -- all tests pass.
    Run: `grep -n "sources_circuit" setup.py` returns no hits.
    Run: `grep -n "iqs/src" CMakeLists.txt` returns no hits.
    Run C tests: `cd build && ctest --output-on-failure` -- all pass.
  </verify>
  <done>
    Dead `sources_circuit` list removed from setup.py. Stale `iqs/src` include path removed from root CMakeLists.txt. Full Python test suite and C test suite pass with zero behavioral changes from all Phase 13 Plan 02 work.
  </done>
</task>

</tasks>

<verification>
1. `pip install -e .` compiles without errors
2. `python -m pytest tests/ -x -q` -- all tests pass
3. `grep -rn "^[[:space:]]*#.*print" cbqs/Model.pyx cbqs/Expression.pyx cbqs/state.pyx cbqs/Constraint.pyx cbqs/state_sampler.pyx cbqs/SearchLib.pyx | wc -l` returns 0 or near-0
4. `grep -n "except:" cbqs/*.pyx` shows no bare except clauses
5. `grep -n "sources_circuit" setup.py` returns no hits
6. `grep -n "iqs/src" CMakeLists.txt` returns no hits
7. No unguarded print() calls remain in Model.pyx approximate_benchmarking or solution property
</verification>

<success_criteria>
- All commented-out code removed from 6 .pyx files
- Unguarded print() calls removed from Model.pyx production code
- CLEAN-03 verified: no bare except clauses in production .pyx files
- Dead sources_circuit list removed from setup.py
- Stale iqs/src include path removed from CMakeLists.txt
- Full test suite passes with zero behavioral changes
</success_criteria>

<output>
After completion, create `.planning/phases/13-dead-code-documentation-cleanup/13-02-SUMMARY.md`
</output>
