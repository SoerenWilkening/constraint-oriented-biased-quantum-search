---
phase: 25-feature-extraction-ml-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - setup.py
  - cbqs/ml/__init__.py
  - cbqs/ml/features.py
  - cbqs/ml/training.py
  - cbqs/ml/adaptation.py
  - tests/test_ml_import.py
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03]

must_haves:
  truths:
    - "`pip install cbqs[ml]` installs scikit-learn as an extra dependency"
    - "`from cbqs import Model` succeeds without sklearn installed"
    - "`from cbqs.ml import FeatureExtractor` raises clear ImportError when sklearn is missing"
    - "`from cbqs.ml import FeatureExtractor` succeeds when sklearn is installed"
  artifacts:
    - cbqs/ml/__init__.py
    - cbqs/ml/features.py
    - cbqs/ml/training.py
    - cbqs/ml/adaptation.py
    - tests/test_ml_import.py
  key_links:
    - "setup.py extras_require includes 'ml' key with scikit-learn"
    - "cbqs/__init__.py does NOT import cbqs.ml"
    - "cbqs/ml/__init__.py checks sklearn availability and raises descriptive ImportError"
---

<objective>
Create the cbqs.ml subpackage skeleton with optional sklearn dependency wiring, import guards, and stub modules for future phases.

Purpose: Establish the ML module structure so feature extraction code has a home, and ensure the optional dependency pattern is correctly wired end-to-end without breaking existing non-ML imports.
Output: cbqs/ml/ subpackage with __init__.py, features.py stub, training.py stub, adaptation.py stub, updated setup.py, and import tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@cbqs/__init__.py
@setup.py

<interfaces>
From cbqs/__init__.py (current state -- must NOT be modified to import ml):
```python
try:
    from .Model import Model
except ImportError as e:
    Model = None

from .Constants import *
from .result import OptimizeResult

__version__ = '2.1.0'
```

From setup.py (current extras_require):
```python
extras_require={
    "test": ["pytest>=7.0"],
    "dev": ["pytest>=7.0", "Cython>=3.0"],
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire optional sklearn dependency and create ml subpackage skeleton</name>
  <files>
    setup.py
    cbqs/ml/__init__.py
    cbqs/ml/features.py
    cbqs/ml/training.py
    cbqs/ml/adaptation.py
  </files>
  <action>
1. **Update setup.py** — Add `"ml": ["scikit-learn>=1.2"]` to the `extras_require` dict alongside existing "test" and "dev" entries. Do NOT modify any other part of setup.py.

2. **Create cbqs/ml/__init__.py** with:
   - A try/except block that imports sklearn. On ImportError, raise a new ImportError with the message: `"The cbqs.ml module requires scikit-learn. Install it with: pip install cbqs[ml]"` using `from None` to suppress the chained exception.
   - After the sklearn check passes, import FeatureExtractor from `.features` and re-export it.
   - Set `__all__ = ["FeatureExtractor"]`.

3. **Create cbqs/ml/features.py** with:
   - A placeholder `FeatureExtractor` class with a docstring explaining it will be implemented in Plan 02.
   - Two stub methods: `extract_variable_features(self, model)` and `extract_instance_features(self, model)` that both raise `NotImplementedError("Feature extraction not yet implemented")`.

4. **Create cbqs/ml/training.py** with:
   - A module docstring: `"""Offline training pipeline for branching weight prediction. Planned for Phase 26."""`
   - No classes or functions (pure stub).

5. **Create cbqs/ml/adaptation.py** with:
   - A module docstring: `"""Online adaptive solve with EMA weight updates. Planned for Phase 27."""`
   - No classes or functions (pure stub).

CRITICAL: Do NOT modify cbqs/__init__.py. The main package must never import cbqs.ml.
  </action>
  <verify>
    <automated>python3 -c "from cbqs import Model; print('OK: cbqs import works')" && python3 -c "from cbqs.ml import FeatureExtractor; print('OK: ml import works')" && python3 -c "import cbqs.ml.training; print('OK: training stub exists')" && python3 -c "import cbqs.ml.adaptation; print('OK: adaptation stub exists')"</automated>
  </verify>
  <done>
    - setup.py has "ml" extra with scikit-learn>=1.2
    - cbqs/ml/__init__.py exists with sklearn import guard
    - cbqs/ml/features.py exists with stub FeatureExtractor class
    - cbqs/ml/training.py and cbqs/ml/adaptation.py exist as stubs
    - `from cbqs import Model` still works (no regression)
    - `from cbqs.ml import FeatureExtractor` works when sklearn is installed
  </done>
</task>

<task type="auto">
  <name>Task 2: Write import isolation tests</name>
  <files>
    tests/test_ml_import.py
  </files>
  <action>
Create tests/test_ml_import.py with pytest tests covering the import isolation requirements:

1. **test_cbqs_import_without_ml** — `from cbqs import Model` succeeds and Model is not None. `from cbqs import OptimizeResult` succeeds. Neither import triggers sklearn import.

2. **test_ml_import_with_sklearn** — `from cbqs.ml import FeatureExtractor` succeeds. `from cbqs.ml.features import FeatureExtractor` succeeds. FeatureExtractor is a class (not None).

3. **test_ml_subpackage_structure** — `import cbqs.ml.training` succeeds. `import cbqs.ml.adaptation` succeeds.

4. **test_ml_import_error_message** — Use `unittest.mock.patch.dict('sys.modules', {'sklearn': None})` to simulate missing sklearn. Verify that importing cbqs.ml raises ImportError with "pip install cbqs[ml]" in the message. Use importlib.reload to re-trigger the import after patching.

5. **test_feature_extractor_stub_raises** — Verify FeatureExtractor().extract_variable_features(None) and .extract_instance_features(None) both raise NotImplementedError.

Use standard pytest patterns. No fixtures needed beyond what's imported.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_ml_import.py -v</automated>
  </verify>
  <done>
    - All 5 tests pass
    - Import isolation verified: cbqs works without sklearn
    - Import error message includes installation instructions
    - Stub methods raise NotImplementedError
  </done>
</task>

</tasks>

<verification>
- `pip install -e .[ml]` installs sklearn alongside cbqs
- `python3 -c "from cbqs import Model"` works in a fresh environment without sklearn
- `python3 -c "from cbqs.ml import FeatureExtractor"` works with sklearn installed
- `python3 -m pytest tests/test_ml_import.py -v` — all tests pass
- cbqs/__init__.py is UNCHANGED from before this plan
</verification>

<success_criteria>
- INTG-01: setup.py extras_require has ml key with scikit-learn
- INTG-02: `from cbqs import Model` works without sklearn (no ImportError)
- INTG-03: Importing cbqs.ml without sklearn gives clear error with "pip install cbqs[ml]"
- Package structure established for Phases 26-27 stubs
- All import tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/25-feature-extraction-ml-foundation/25-01-SUMMARY.md`
</output>
