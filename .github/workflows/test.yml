name: Tests

on:
  push:
    branches: ['*']
  pull_request:
    branches: [main]

jobs:
  c-tests:
    name: C Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential

      - name: Configure
        run: mkdir build-tests && cd build-tests && cmake ../tests

      - name: Build
        run: cd build-tests && make -j$(nproc)

      - name: Test
        run: cd build-tests && ctest -R "test_(intarray|expression|state|constraint|model|branching|solver|searchlib|integration)" --output-on-failure

  c-tests-asan:
    name: C Tests (AddressSanitizer)
    runs-on: ubuntu-latest
    env:
      # Leak detection disabled: preprocessing() has known leaks (tracked for Phase 2)
      ASAN_OPTIONS: detect_leaks=0:abort_on_error=1
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential

      - name: Configure with ASan
        run: mkdir build-tests-asan && cd build-tests-asan && cmake ../tests -DASAN=ON

      - name: Build
        run: cd build-tests-asan && make -j$(nproc)

      - name: Test
        run: cd build-tests-asan && ctest -R "test_(intarray|expression|state|constraint|model|branching|solver|searchlib|integration)" --output-on-failure

  c-tests-valgrind:
    name: C Tests (Valgrind)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential valgrind

      - name: Configure
        run: mkdir build-tests-valgrind && cd build-tests-valgrind && cmake ../tests

      - name: Build
        run: cd build-tests-valgrind && make -j$(nproc)

      - name: Test with Valgrind
        run: |
          cd build-tests-valgrind
          # Run each test under Valgrind
          # Note: Valgrind is slow, so we run a subset of critical tests
          for test in test_expression test_state test_constraint; do
            if [ -f "./$test" ]; then
              valgrind --leak-check=full --error-exitcode=1 --errors-for-leak-kinds=definite ./$test
            fi
          done

  c-tests-tsan:
    name: C Tests (ThreadSanitizer)
    runs-on: ubuntu-latest
    env:
      TSAN_OPTIONS: halt_on_error=1
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y cmake clang

      - name: Configure with ThreadSanitizer
        run: |
          mkdir build-tests-tsan && cd build-tests-tsan
          cmake ../tests \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_C_FLAGS="-fsanitize=thread -g -O1" \
            -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=thread"

      - name: Build
        run: cd build-tests-tsan && make -j$(nproc)

      - name: Run thread safety tests with TSan
        run: |
          cd build-tests-tsan
          ctest -R test_thread_safety -V --output-on-failure

  python-tests:
    name: Python Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install build dependencies
        run: pip install cython numpy setuptools

      - name: Install package
        run: pip install --no-build-isolation -e .

      - name: Install test dependencies
        run: pip install pytest pytest-timeout joblib gurobipy

      - name: Run unit tests
        run: pytest tests/ -v --tb=short --ignore=tests/test_stress.py

      - name: Run stress tests
        run: pytest tests/test_stress.py -v --timeout=180
