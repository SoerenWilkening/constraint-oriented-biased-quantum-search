#ifndef TYPEDEFS_H
#define TYPEDEFS_H

#define minimize 1
#define maximize (-1)
#define lessequal -1
#define equal 0
#define greaterequal 1

#define MIN(a,b)                ((a) < (b) ? (a) : (b))
#define false 0
#define true 1
#define undetermined 2

#endif